import type { Options } from "prettier";

const config: Options = {
  arrowParens: "always",
  bracketSpacing: true,
  insertPragma: false,
  printWidth: 80,
  proseWrap: "preserve",
  quoteProps: "as-needed",
  requirePragma: false,
  endOfLine: "auto",
  semi: true,
  singleQuote: false,
  tabWidth: 2,
  trailingComma: "all",
  useTabs: false,
};

export default config;
